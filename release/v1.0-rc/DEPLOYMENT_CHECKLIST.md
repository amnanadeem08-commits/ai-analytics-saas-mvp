# v1.0 RC — Deployment Checklist

- [ ] All configuration checklist items complete
- [ ] `pip install -r requirements.txt`
- [ ] `python -m compileall backend`
- [ ] API started with process manager
- [ ] Worker started (if async jobs enabled)
- [ ] `/api/v1/live` returns 200
- [ ] `/api/v1/ready` returns ready
- [ ] `/api/v1/release/validation` subsystem checks pass
- [ ] Monitoring dashboards connected
