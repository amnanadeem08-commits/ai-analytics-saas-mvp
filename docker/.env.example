# Data Bot AI — environment template (Sprint 8.5)
# Copy to .env and adjust for local / production profiles.

APP_ENV=development
APP_NAME=AI Analytics SaaS MVP
API_VERSION=1.0.0

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json

# Security (required in production — min 16 chars)
JWT_SECRET=change-me-in-production-min-16
SECRET_KEY=

# Database persistence backend (memory | postgres | sqlite)
STORAGE_BACKEND=postgres
DATABASE_URL=postgresql://databot:databot@postgres:5432/databot

# Queue / worker
QUEUE_BACKEND=redis
REDIS_URL=redis://redis:6379/0
WORKER_ENABLED=true

# Object storage provider (local | s3)
OBJECT_STORAGE_BACKEND=local
STORAGE_ROOT_DIR=/app/data/storage

# Upload limits
MAX_UPLOAD_SIZE_MB=200

# CORS (Sprint 8.7)
CORS_ALLOWED_ORIGINS=*
CORS_ALLOW_CREDENTIALS=false

# Security hardening (Sprint 8.7)
RATE_LIMIT_REQUESTS=120
RATE_LIMIT_WINDOW_SECONDS=60
AUTH_MAX_FAILED_ATTEMPTS=5
AUTH_LOCKOUT_SECONDS=300
CSRF_ENABLED=false
SECURITY_HSTS_ENABLED=false
SECURITY_CSP_ENABLED=true

# Frontend
API_BASE_URL=http://backend:8000

# Observability
METRICS_ENABLED=true
TRACING_ENABLED=true
